# 代码修改记录

- **日期**: 2026-07-06
- **修改人**: CGJ-666
- **需求**: OSS云存储补偿上传 — 数据库有故障原图路径但OSS中无对应文件，需提供补偿上传机制并修复实时图像管线

---

## 涉及文件

| 文件 | 操作 |
|------|------|
| `hotspot_api/apps/logs/services.py` | 修改 |
| `hotspot_api/apps/history/views.py` | 修改 |
| `hotspot_api/apps/history/urls.py` | 修改 |
| `hotspot_api/apps/history/management/commands/scan_missing_images.py` | 新建 |
| `hotspot_main/hotspot_api/settings.py` | 修改 |
| `hotspot_main/apps/monitor/oss_utils.py` | 新建 |
| `hotspot_main/apps/monitor/services.py` | 修改 |

---

## 一、`hotspot_api/apps/logs/services.py`

### 新增代码

**新增 `LogService.upload_to_oss()` 静态方法**（位于 `upload_image()` 方法之后，`export_logs_to_excel()` 之前）：

```python
@staticmethod
def upload_to_oss(file_bytes: bytes, object_key: str) -> str:
    """
    上传文件到阿里云 OSS，返回 OSS object key。

    Args:
        file_bytes: 文件字节数据
        object_key: OSS 对象键，如 'alarm_images/1/2026-07-06/xxx.jpg'

    Returns:
        str: 上传成功的 OSS object key

    Raises:
        ValueError: OSS 配置缺失
        RuntimeError: 上传失败
    """
    oss_config = getattr(settings, 'OSS_CONFIG', None)
    if not oss_config or not oss_config.get('ACCESS_KEY_ID'):
        raise ValueError('OSS_CONFIG 未配置，无法上传')

    import oss2
    auth = oss2.Auth(oss_config['ACCESS_KEY_ID'], oss_config['ACCESS_KEY_SECRET'])
    bucket = oss2.Bucket(auth, oss_config['ENDPOINT'], oss_config['BUCKET_NAME'])

    result = bucket.put_object(object_key, BytesIO(file_bytes))
    if result.status != 200:
        raise RuntimeError(f'OSS 上传失败，状态码: {result.status}')

    return object_key
```

---

## 二、`hotspot_api/apps/history/views.py`

### 删除/修改代码

无删除。以下为新增内容。

### 新增代码

**1. 文件头部新增 `import os`**：
```python
import os
```

**2. `HistoryAlarmViewSet` 类新增 `upload_alarm_image` action**（位于 `_generate_presigned_url()` 之后，`export_alarm()` 之前）：

```python
@action(detail=False, methods=['post'], url_path='upload-image')
def upload_alarm_image(self, request):
    """
    POST /api/history/alarm/upload-image/
    补偿上传：为指定告警记录上传故障图片到 OSS 并更新 image_path。
    """
    alarm_id = request.POST.get('alarm_id')
    image_file = request.FILES.get('image')
    image_type = request.POST.get('image_type', 'original')

    if not alarm_id:
        return Result.error(code=400, msg='缺少必填参数: alarm_id')
    if not image_file:
        return Result.error(code=400, msg='未上传图片文件')
    try:
        alarm_id = int(alarm_id)
    except (ValueError, TypeError):
        return Result.error(code=400, msg='alarm_id 格式错误')

    try:
        alarm = AlarmRecord.objects.get(pk=alarm_id)
    except AlarmRecord.DoesNotExist:
        return Result.error(code=404, msg=f'告警记录不存在: id={alarm_id}')

    ext = os.path.splitext(image_file.name)[1].lower()
    allowed_exts = {'.jpg', '.jpeg', '.png', '.webp'}
    if ext not in allowed_exts:
        return Result.error(code=400, msg=f'不支持的图片格式：{ext}，仅支持 jpg/png/webp')
    if image_file.size > 5 * 1024 * 1024:
        return Result.error(code=400, msg='图片大小不能超过 5MB')

    image_bytes = image_file.read()

    from django.utils import timezone as tz
    now = tz.now()
    date_str = now.strftime('%Y-%m-%d')
    time_str = now.strftime('%Y%m%d_%H%M%S')
    branch = alarm.branch
    suffix = '_annotated' if image_type == 'annotated' else ''
    object_key = f'alarm_images/{branch}/{date_str}/{alarm_id}_{time_str}{suffix}{ext}'

    try:
        from apps.logs.services import LogService
        uploaded_key = LogService.upload_to_oss(image_bytes, object_key)
    except ValueError as e:
        return Result.error(code=500, msg=str(e))
    except RuntimeError as e:
        return Result.error(code=500, msg=str(e))

    if image_type == 'annotated':
        alarm.annotated_image_path = uploaded_key
        alarm.save(update_fields=['annotated_image_path', 'updated_at'])
    else:
        alarm.image_path = uploaded_key
        alarm.save(update_fields=['image_path', 'updated_at'])

    logger.info(f'告警 {alarm_id} 的 {image_type} 图片已补偿上传: {uploaded_key}')
    return Result.success(
        msg='图片上传成功',
        data={
            'alarm_id': alarm.id,
            'object_key': uploaded_key,
            'image_type': image_type,
        },
    )
```

**3. 类文档字符串更新**（新增一行）：
```
- POST /api/history/alarm/upload-image/ → 补偿上传故障图片到 OSS
```

**4. 章节编号更新**：原 `3.5 告警历史导出` 编号调整为 `3.6`

---

## 三、`hotspot_api/apps/history/urls.py`

### 删除代码

无删除。

### 新增代码

**新增路由映射和 URL 路径**：

```python
# 新增映射
alarm_upload_image = HistoryAlarmViewSet.as_view({'post': 'upload_alarm_image'})

# 新增路由
path('history/alarm/upload-image/', alarm_upload_image, name='history-alarm-upload-image'),
```

---

## 四、`hotspot_api/apps/history/management/commands/scan_missing_images.py`（新建文件）

### 新增代码

完整新建文件，包含 `Command` 类，提供：
- **扫描模式**: `python manage.py scan_missing_images` — 扫描 `alarm_records` 表中 `image_path` 或 `annotated_image_path` 为空的记录
- **补录模式**: `python manage.py scan_missing_images --alarm-id <ID> --image-file <PATH>` — 为单条记录补偿上传图片到 OSS

支持参数: `--type` (original/annotated/all), `--limit`, `--show-resolved`, `--alarm-id`, `--image-file`

---

## 五、`hotspot_main/hotspot_api/settings.py`

### 删除代码

无删除。

### 新增代码

在文件末尾 `AUTH_USER_MODEL = 'users.User'` 之后新增：

```python
# ── 文件存储 ──
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# ── 阿里云 OSS 配置（真实密钥放在 local_settings.py 中覆盖） ──
OSS_CONFIG = {
    'ACCESS_KEY_ID': '',
    'ACCESS_KEY_SECRET': '',
    'ENDPOINT': '',
    'BUCKET_NAME': '',
}

# 本地配置覆盖
try:
    from .local_settings import *  # noqa: F403
except ImportError:
    pass
```

---

## 六、`hotspot_main/apps/monitor/oss_utils.py`（新建文件）

### 新增代码

完整新建文件，包含三个工具函数：
- `upload_bytes_to_oss(file_bytes, object_key)` — 上传字节数据到阿里云 OSS
- `generate_oss_key(device_id, ts, suffix)` — 生成 OSS 对象键
- `save_image_locally(file_bytes, relative_path)` — 保存到本地 MEDIA_ROOT 作为备份

---

## 七、`hotspot_main/apps/monitor/services.py`

### 删除代码

**`MQTTDataProcessor.process_image()` 中**，原创建 ThermalImage 记录代码：
```python
# 存储图像记录
ThermalImage.objects.create(
    timestamp=ts,
    device_id=device_id,
    image_path='',  # Base64 数据不存文件路径，直接通过 SSE 推送
    annotated_path='',
    width=width,
    height=height,
    hotspots=hotspots,
)
```

### 新增代码

**1. `MQTTDataProcessor.process_image()` 中**，替换为 OSS 上传 + 本地备份 + 数据库记录：
```python
# 存储图像记录（上传到 OSS + 本地备份）
from .oss_utils import (
    upload_bytes_to_oss, generate_oss_key, save_image_locally,
)
import base64 as b64

oss_image_key = ''
oss_annotated_key = ''

# 处理原始红外图像
if image_b64:
    try:
        image_bytes = b64.b64decode(image_b64)
        oss_key = generate_oss_key(device_id, ts, suffix='.jpg')
        result = upload_bytes_to_oss(image_bytes, oss_key)
        if result:
            oss_image_key = result
        # 本地备份
        local_ts = timezone.localtime(ts)
        date_str = local_ts.strftime('%Y-%m-%d')
        time_str = local_ts.strftime('%Y%m%d_%H%M%S')
        local_rel_path = f'alarm_images/{device_id}/{date_str}/{time_str}_{device_id}.jpg'
        save_image_locally(image_bytes, local_rel_path)
    except Exception as e:
        logger.error(f'处理原始红外图像失败: {e}')

# 处理标注图像
if annotated_b64:
    try:
        annotated_bytes = b64.b64decode(annotated_b64)
        oss_ann_key = generate_oss_key(device_id, ts, suffix='_annotated.jpg')
        result = upload_bytes_to_oss(annotated_bytes, oss_ann_key)
        if result:
            oss_annotated_key = result
    except Exception as e:
        logger.error(f'处理标注图像失败: {e}')

ThermalImage.objects.create(
    timestamp=ts,
    device_id=device_id,
    image_path=oss_image_key,
    annotated_path=oss_annotated_key,
    width=width,
    height=height,
    hotspots=hotspots,
)
```

**2. `AlarmService.check_and_trigger()` 中**，在 `now = timezone.now()` 之后、`alarm = AlarmRecord.objects.create(...)` 之前，新增热像图关联逻辑：
```python
# 如果没有传入 image_path，尝试关联最近的红外热像图
if not image_path:
    recent_image = ThermalImage.objects.filter(
        timestamp__gte=now - timedelta(seconds=60),
    ).exclude(
        image_path='',
    ).order_by('-timestamp').first()

    if recent_image:
        image_path = recent_image.image_path
        logger.info(
            f'告警 {alarm_type} 关联图像: {image_path} '
            f'(ThermalImage id={recent_image.id})'
        )
```

---

## 数据库变更

无数据库结构变更。所有改动仅涉及：
- 新增 Python 代码（工具方法、API 接口、管理命令）
- 修改现有 Python 代码逻辑（开始存储 image_path 而非空字符串）
- 更新 `alarm_records` 表中已有记录的 `image_path` 字段（通过补偿上传接口）
- 使用现有数据库字段，未增删任何字段/表/索引/约束
